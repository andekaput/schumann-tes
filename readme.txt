The folder contains:

setup.py — defines classes and functions related to the physical and mathematical calculations.

main_charge_discharge.py — contains the main() function used to run the model.

cascetta_results.csv — experimental data taken from Cascetta et al. (2016).
